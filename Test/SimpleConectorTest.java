package Test;

/*import logica.Flecha;
import logica.SimpleConector;
import org.junit.Rule;
import org.junit.Test;
import org.junit.contrib.java.lang.system.SystemOutRule;

import static org.junit.Assert.assertTrue;

public class SimpleConectorTest {

    @Rule
    public final SystemOutRule systemOutRule = new SystemOutRule().enableLog();

    @Test
    public void testDraw() {
        // Crear un objeto SimpleConector para probar
        SimpleConector simpleConector = new SimpleConector(0, 0, Flecha.SIMPLE, true, 1, 1);

        // Llamar al método draw
        simpleConector.draw();

        // Verificar la salida esperada
        assertTrue(systemOutRule.getLog().contains("Dibujando flecha de tipo SIMPLE inicia en coordenada(0, 0) con número de serie true con relleno true y tipo de punta triangular, finaliza en la coordenada (1,1)"));
    }

    // Otras pruebas...
}

*/