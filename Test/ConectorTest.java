/*package Test;

import org.junit.Test;
import static org.junit.Assert.*;
import logica.*;

public class SimpleConectorTest {

    @Test
    public void testDraw() {
        SimpleConector simpleConector = new SimpleConector(0, 0, Flecha.SIMPLE, true, 1, 1);

        assertTrue("Dibujando flecha de tipo SIMPLE inicia en coordenada(0, 0) con número de serie true con relleno true y tipo de punta triangular, finaliza en la coordenada (1,1)", simpleConector.draw());
    }

    @Test
    public void testSetSerialNumber() {
        SimpleConector simpleConector = new SimpleConector(0, 0, Flecha.SIMPLE, true, 1, 1);

        simpleConector.setSerialNumber(false);
        assertFalse(simpleConector.isSerialNumber());
    }

}
*/